<%= form_with(model: fishing, local: true) do |form| %>
  <% if fishing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fishing.errors.count, "error") %> prohibited this fishing from being saved:</h2>

      <ul>
        <% fishing.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :author_id %>
    <%= form.number_field :author_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="field">
    <%= form.label :imagen %>
    <%= form.text_field :imagen %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
